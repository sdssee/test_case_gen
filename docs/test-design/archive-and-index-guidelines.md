# 归档说明

每个用户指定范围的新运行固定在 `docs/test-design/current/<run-id>/`，两个 Excel 留在该 run-dir 的 `deliverables/`。默认不向公共目录复制、不压缩、不更新产品级索引。已存在事实文件的历史运行可以原地恢复，但交付回执必须返回实际完整路径。

用户明确要求产品事实归档时，只增量写入稳定、已由页面操作确认、无敏感信息且不属于 `open_items` 的事实。补充测试继续使用同一 run-dir 追加完整事务并重建下游结构化产物；不得直接修改 Excel 绕过事实和计划。
