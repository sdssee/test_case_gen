# test_case_gen

面向 CodeBuddy 的项目级测试设计规范包，用于统一测试设计、测试用例生成、页面元素覆盖、性能测试规划和自动化建议的交付格式。

本仓库不是传统应用代码项目，而是一组可复制到业务项目根目录的 CodeBuddy 配置、规则、Skill 和 Excel 模板。导入后，CodeBuddy 可以按照统一规范理解需求、拆解测试场景，结合需求文档、截图、原型或可访问页面识别所有可点击功能，并生成结构化测试设计 Excel。

## 适用场景

- 敏捷用户故事测试设计
- 需求文档测试分析
- 接口文档测试用例生成
- PR Diff 或代码变更影响分析
- 缺陷单回归测试设计
- 原型图或页面说明测试设计
- 页面截图或可访问页面的全量可点击功能覆盖
- 既有测试用例补充与优化
- 混合输入材料的测试分析

## 核心能力

- 提供项目级 `CODEBUDDY.md` Memory，固定团队测试设计偏好。
- 提供 `test-design` Skill，指导 CodeBuddy 按标准流程完成测试设计。
- 提供硬性质量 Rule，约束用例数量、字段格式、覆盖维度和 Excel 输出。
- 提供标准测试设计 Excel 模板，保证正式交付物结构一致。
- 支持根据文档、截图、原型、页面 URL 或浏览器/computer use 实探补全页面元素覆盖清单。
- 支持用设计文档、需求说明、接口文档和验收标准推导页面业务逻辑，再结合页面实际入口生成测试用例。
- 覆盖功能测试、页面元素覆盖、性能测试、风险识别、待确认问题、自动化建议和测试系统导入粘贴视图。

## 目录结构

```text
test_case_gen/
├── CODEBUDDY.md
├── README.md
├── README_IMPORT.md
├── .codebuddy/
│   ├── skills/
│   │   └── test-design/
│   │       └── SKILL.md
│   ├── .rules/
│   │   └── test-design-rule.mdc
│   └── rules/
│       └── test-design-rule.md
└── docs/
    └── test-design/
        ├── README.md
        ├── excel-template-spec.md
        ├── codebuddy-test-design-template.xlsx
        ├── 测试用例模板.xlsx
        ├── 1.jpg
        ├── 2.jpg
        └── 3.jpg
```

## 关键文件

| 文件 | 说明 |
| --- | --- |
| `CODEBUDDY.md` | 项目级 Memory，声明长期测试设计偏好。 |
| `.codebuddy/skills/test-design/SKILL.md` | 测试设计 Skill，定义角色、输入识别、工作流、输出要求和自检规则。 |
| `.codebuddy/.rules/test-design-rule.mdc` | CodeBuddy IDE 规则文件。 |
| `.codebuddy/rules/test-design-rule.md` | CodeBuddy Code/CLI 兼容规则文件。 |
| `docs/test-design/codebuddy-test-design-template.xlsx` | 正式测试设计 Excel 模板。 |
| `docs/test-design/测试用例模板.xlsx` | 测试系统导出的导入模板参考文件，`测试系统导入用例` Sheet 必须对齐它的字段顺序。 |
| `docs/test-design/1.jpg`、`2.jpg`、`3.jpg` | 测试系统字段解释截图。 |
| `docs/test-design/excel-template-spec.md` | Excel 模板字段说明。 |
| `README_IMPORT.md` | 导入到其他项目的操作说明。 |

## Excel 模板 Sheet

正式测试设计交付物默认包含 9 个 Sheet：

1. `测试设计总览`
2. `需求用户故事拆解`
3. `测试场景矩阵`
4. `功能测试用例`
5. `性能测试设计`
6. `风险与待确认问题`
7. `自动化建议`
8. `页面元素覆盖清单`
9. `测试系统导入用例`

## 测试设计工作流

CodeBuddy 使用本规范时，应按以下流程完成测试设计：

1. 识别输入类型，例如用户故事、需求文档、接口文档、PR Diff、缺陷单、截图、原型或页面 URL。
2. 提取业务目标、角色、功能点、验收标准、业务规则、依赖系统和待确认问题。
3. 从设计文档、需求说明、接口文档和验收标准中提取业务逻辑，例如角色权限、状态流转、字段规则、异常规则、数据影响、通知、日志和审计。
4. 如果输入包含截图、原型、页面 URL 或页面说明，识别页面入口、所有按钮/链接/菜单/图标按钮/筛选/分页/弹窗等可交互元素。
5. 如果文档或截图不足，并且存在可访问页面，使用浏览器或 computer use 实探页面行为。
6. 如果用户提供测试数据，优先用这些数据进行页面实探和用例设计；正常数据用于校准成功路径预期，异常/边界数据用于补充负向测试。
7. 将业务逻辑和页面交互融合：文档说明业务意图，页面补齐具体入口；页面出现文档未说明的入口时，写基础交互用例并登记待确认业务规则。
8. 页面实探时只能新增、修改、删除或变更自己创建的测试数据；对页面已有数据可以查看、搜索、筛选、排序、分页、打开详情，或进入编辑/修改页面观察字段、校验和交互，但不得保存、提交或改变已有数据。
9. 明确测试范围、不测范围、回归范围、测试环境、准入条件和准出条件。
10. 从权限、边界、状态流转、幂等性、数据一致性、第三方依赖、性能和安全等维度识别风险。
11. 使用等价类、边界值、判定表、状态迁移、场景法、风险驱动测试等方法建立测试场景矩阵。
12. 生成可执行、可验证、可追踪的功能测试用例，并确保页面所有可点击/可交互功能均有关联用例或明确不测原因。
13. 按模块、页面、业务流程和小功能块编排功能测试用例，同一小功能块的用例连续放在附近。
14. 生成 `测试系统导入用例` Sheet，字段顺序与 `docs/test-design/测试用例模板.xlsx` 一致，便于复制粘贴到测试系统。
15. 输出性能测试设计；如果不适用，也必须说明原因和后续监控建议。
16. 给出自动化建议，包括自动化层级、价值、优先级、依赖数据、Mock 需求和稳定性风险。
17. 交付前自检覆盖完整性、页面元素覆盖率、用例质量、编号换行格式、重复用例和待确认问题。

## 质量规则摘要

- 正式测试设计交付物必须是 Excel。
- 任何测试设计都必须包含性能测试内容。
- 敏捷用户故事每条至少生成 10 条功能测试用例。
- `页面元素覆盖清单` 只是覆盖追踪矩阵，不是测试用例 Sheet；所有功能测试用例必须写入 `功能测试用例` Sheet，所有性能测试场景必须写入 `性能测试设计` Sheet。
- `测试系统导入用例` 是从 `功能测试用例` 派生的粘贴视图，字段顺序必须与测试系统导出的 `测试用例模板.xlsx` 一致。
- 测试系统模板中的红色字段和必填字段必须按每条测试用例动态生成，不得固定写示例值或通用占位值。
- 页面实探只能新增、修改、删除或变更自己创建的测试数据；对已有数据可以进入详情或编辑/修改页面观察字段和交互，但不能保存、提交或改变已有数据。
- 功能测试用例必须按小功能块连续编排，同一块功能的用例放在附近。
- 用户提供测试数据时，必须优先用于页面实探和测试用例设计；页面真实提示、校验、跳转和状态变化应反映到操作步骤和预期结果中。
- `前置条件`、`操作步骤`、`预期结果` 必须使用 `1. 2. 3.` 编号换行。
- 用例必须具备明确前置条件、测试数据、操作步骤和可验证预期结果。
- 每条测试场景和测试用例都必须关联 Story ID 或需求 ID、功能点、测试维度和风险或验收标准。
- 涉及页面、截图、原型或可访问系统时，必须输出 `页面元素覆盖清单`，页面所有可点击/可交互元素必须关联用例、标记不适用、标记不测范围或登记待确认问题。
- 不允许通过重复、拆碎或同质化用例凑数量。

## 使用方式

将本仓库内容复制到目标项目根目录后，可以对 CodeBuddy 使用以下提示词：

```text
请使用项目级 test-design Skill，并遵守项目测试设计 Rule。
根据以下用户故事生成测试设计 Excel，模板使用 docs/test-design/codebuddy-test-design-template.xlsx。
要求每条用户故事至少 10 条功能测试用例，必须包含性能测试设计。
```

处理页面截图或可访问页面时：

```text
请使用项目级 test-design Skill，并遵守项目测试设计 Rule。
我会提供需求文档和页面截图/页面地址，请先识别页面所有可点击和可交互元素，再生成测试设计 Excel。
要求功能测试用例覆盖所有按钮、链接、菜单、图标按钮、筛选、分页、弹窗和表格行操作；无法确认的元素写入页面元素覆盖清单和待确认问题。
页面具体业务逻辑请参考设计文档、需求说明、接口文档和验收标准；如果文档只写大概功能，请结合页面入口补全测试场景，并把无法确认的业务规则登记为待确认问题。
页面元素覆盖清单仅用于记录页面元素、覆盖状态和关联用例 ID；测试步骤和预期结果必须写入功能测试用例 Sheet，性能场景必须写入性能测试设计 Sheet。
请生成测试系统导入用例 Sheet，字段顺序与 docs/test-design/测试用例模板.xlsx 保持一致，内容从功能测试用例 Sheet 映射，便于复制粘贴到测试系统。
测试系统导入模板中的红色字段和必填字段必须根据每条测试用例动态填写；测试用例名称、步骤描述、预期结果、测试类型、用例级别、执行方式不能固定套示例值。
测试系统导入模板中带下拉框的字段只能填写下拉允许值：测试类型为功能测试、性能规格测试、可靠性测试、兼容性测试、可维护性测试、安全性测试、易用性测试；测试用例级别为 L1、L2、L3、L4；执行方式为自动化、手动。测试系统自动生成字段不需要填写。
页面实探时，对已有数据可以查看、搜索、筛选、排序、分页、打开详情，或进入编辑/修改页面观察字段、校验和交互，但不能保存、提交或改变已有数据；如需新增/编辑/删除/状态变更，只能操作带有 `AI_TEST` 或 `CODEX_TEST` 等标识且由本次实探创建的数据。
如果我提供测试数据，请优先拿这些数据进行实际页面操作；正常数据用于确认成功路径，异常/边界数据用于观察页面真实校验、错误提示和恢复路径，最终用例中的敏感数据请脱敏。
功能测试用例请按小功能块连续编排，同一块功能的主流程、异常、边界、权限/状态等用例放在附近。
```

也可以使用更简短的方式：

```text
基于项目测试设计规范，为以下用户故事生成测试设计 Excel。
```

## Codex 使用说明

本仓库已提供 `AGENTS.md`，Codex 运行时应优先读取该文件，并继续复用 `.codebuddy/skills/test-design/SKILL.md`、Rule、`CODEBUDDY.md` 和 Excel 模板规范。

如果用 Codex 处理页面截图、设计文档或可访问页面，应遵循同一套规则：业务逻辑从设计文档、需求说明、接口文档和验收标准中抽取，页面截图或浏览器/computer use 实探用于补齐具体入口、控件和交互路径。

Codex 使用浏览器或 computer use 实探页面时，也必须遵守数据安全边界：已有数据可进入详情或编辑/修改页面观察，但不能保存、提交或改变；增删改查仅限自己创建且有明显测试标识的数据。

本仓库约定：每次修改完成并验证通过后，默认提交到当前 Git 分支并推送到 `origin`。

## 维护建议

- 团队测试字段变化时，同步更新 `docs/test-design/excel-template-spec.md` 和 Excel 模板。
- 测试策略变化时，同步更新 `CODEBUDDY.md`、Skill 和 Rule，避免规则不一致。
- 如果确认当前 CodeBuddy 只加载一种规则目录，可以只保留对应规则文件。
- 定期检查 Excel 模板中的示例行、枚举值、冻结表头和自动换行设置。
