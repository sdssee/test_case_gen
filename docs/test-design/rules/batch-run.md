# 大范围运行规则

一个用户指定测试范围使用一个 run-dir 和一个连续会话。范围内按页面或功能区块顺序深探，完成当前事务后再移动到下一部分；不得按最小标题、元素或用例拆分 Agent、子任务、账本或分片。

若范围过大导致会话中断，从同一 `facts.json` 和实际页面状态恢复：先检查本轮测试对象状态，再继续尚未处理的页面功能。只有用户明确切换到不同产品或无关测试目标时才使用新的 run-dir。
