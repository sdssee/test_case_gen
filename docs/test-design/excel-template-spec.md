# 测试设计 Excel 模板规格

## Sheet 1：测试设计总览

字段：

- 项目/模块
- 需求名称
- 版本/迭代
- 测试负责人
- 需求来源
- 测试范围
- 不测范围
- 测试类型
- 测试环境
- 主要风险
- 准入条件
- 准出条件
- 待确认问题

## Sheet 2：需求用户故事拆解

字段：

- Story ID/需求 ID
- 用户故事/需求描述
- 角色
- 业务价值
- 验收标准
- 业务规则
- 前置条件
- 后置影响
- 依赖系统
- 待确认问题

## Sheet 3：测试场景矩阵

字段：

- 场景 ID
- Story ID/需求 ID
- 功能点
- 场景类型
- 正向/反向
- 测试维度
- 风险等级
- 优先级
- 是否生成用例
- 备注

## Sheet 4：功能测试用例

字段：

- 用例 ID
- Story ID/需求 ID
- 模块
- 功能点
- 用例标题
- 优先级
- 测试类型
- 前置条件
- 测试数据
- 操作步骤
- 预期结果
- 实际结果
- 执行状态
- 是否适合自动化
- 关联风险
- 备注

字段格式：

- `前置条件`、`操作步骤`、`预期结果` 必须使用 `1. 2. 3.` 换行分条。
- 示例：

```text
1. 测试环境已部署本次需求版本
2. 测试账号已完成注册且状态为启用
3. 用户已进入登录页
```

## Sheet 5：性能测试设计

字段：

- 性能场景 ID
- Story ID/需求 ID
- 业务链路
- 性能测试类型
- 目标用户量/并发数
- TPS/QPS 目标
- 响应时间目标
- 数据量级
- 测试时长
- 监控指标
- 通过标准
- 造数策略
- 风险说明
- 是否纳入本轮测试

## Sheet 6：风险与待确认问题

字段：

- 编号
- 类型
- 描述
- 影响范围
- 风险等级
- 建议处理方式
- 负责人
- 状态

## Sheet 7：自动化建议

字段：

- 用例 ID/场景 ID
- 自动化层级
- 自动化价值
- 自动化优先级
- 依赖数据
- Mock 需求
- 稳定性风险
- 建议框架/工具
- 备注

## Sheet 8：页面元素覆盖清单

适用场景：

- 输入包含页面截图、原型图、页面 URL、菜单路径、操作录屏或页面说明。
- 需要覆盖页面所有可点击/可交互功能。
- 文档不足时，需要通过浏览器或 computer use 观察页面后补齐元素清单。

页面实探数据安全：

- 对已有数据只能执行查看、搜索、筛选、排序、分页、打开详情等只读操作。
- 新增、编辑、删除或状态变更只允许作用于本次实探自己创建且带有明显测试标识的数据，例如 `AI_TEST`、`CODEX_TEST`、当前日期或任务编号。
- 如果无法确认数据是否由自己创建，必须按已有数据处理。
- 不可逆、高风险、影响资金、权限、生产通知或外部系统的操作，应优先记录为待确认或设计用例，不直接执行。

定位：

- 本 Sheet 是页面元素覆盖追踪矩阵，不是测试用例 Sheet。
- 本 Sheet 只记录页面元素、业务依据、覆盖状态、发现方式、素材来源和关联的 `覆盖用例 ID`。
- 所有功能测试用例必须写入 `功能测试用例` Sheet。
- 所有性能测试场景必须写入 `性能测试设计` Sheet。
- 不得在本 Sheet 中编写独立测试用例、操作步骤、测试数据或完整预期结果正文。

字段：

- 元素 ID
- Story ID/需求 ID
- 页面/入口
- 页面 URL/菜单路径
- 元素名称/文案
- 元素类型
- 交互方式
- 前置状态/权限
- 预期行为
- 业务依据/规则来源
- 覆盖用例 ID
- 覆盖状态
- 发现方式
- 素材来源
- 待确认问题/备注

字段说明：

- `元素类型` 固定值建议：按钮、链接、菜单、Tab、图标按钮、下拉项、输入框、上传控件、开关、复选框、单选框、表格行操作、批量操作、分页、排序、筛选、弹窗按钮、抽屉按钮、快捷入口、其他。
- `交互方式` 固定值建议：点击、双击、悬停、输入、选择、拖拽、上传、下载、快捷键、其他。
- `覆盖状态` 固定值：已覆盖、不适用、不测范围、待确认。
- `发现方式` 固定值建议：需求文档、截图、原型、浏览器实探、computer use、代码/DOM、用户说明。
- `业务依据/规则来源` 用于记录设计文档章节、需求说明、接口文档、验收标准、页面观察或待确认项，避免只按按钮机械生成用例。
- `覆盖用例 ID` 可填写一个或多个功能测试用例 ID；如果覆盖状态不是 `已覆盖`，必须在 `待确认问题/备注` 中说明原因。

覆盖规则：

- 所有可点击/可交互元素必须有覆盖状态。
- `已覆盖` 的元素必须关联功能测试用例。
- `覆盖用例 ID` 必须指向 `功能测试用例` Sheet 中的用例；性能相关元素或链路应通过 `性能测试设计` Sheet 中的性能场景 ID 体现。
- `不适用`、`不测范围`、`待确认` 的元素必须写明原因。
- 页面交互用例必须同时体现业务逻辑依据和具体页面操作。
- 不允许只覆盖页面主流程而遗漏行内按钮、图标按钮、二级菜单、更多操作、分页、排序、筛选、上传、下载、弹窗确认/取消等入口。

## 样式和可用性要求

- 所有 Sheet 表头冻结。
- 所有 Sheet 表头加粗并使用统一底色。
- `前置条件`、`操作步骤`、`预期结果`、`测试数据`、`备注`、`预期行为`、`业务依据/规则来源`、`待确认问题/备注` 等长文本列启用自动换行。
- 表格开启筛选。
- 枚举字段应使用固定值。
- 工作簿内保留示例行，便于用户理解填写方式。
