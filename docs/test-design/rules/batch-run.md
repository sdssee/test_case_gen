# 大范围运行规则

一个用户指定测试范围使用一个 run-dir。范围内按页面或功能区块顺序深探，完成当前事务后再移动到下一部分；允许 4 个阶段 Agent 由用户手动串行接力，但不得按最小标题、元素或用例拆分 Agent、子任务、账本或分片。

若会话或 Agent 中断，从同一固化产物和实际页面状态恢复：先检查本轮测试对象状态，再继续当前阶段尚未处理的功能。只有用户明确切换到不同产品或无关测试目标时才使用新的 run-dir。
