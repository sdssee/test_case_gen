# 用例设计规则

## 交付语言

- 任务开始时确定交付语言；正式测试设计默认使用中文，场景、用例、风险、性能设计和自动化建议等说明性内容必须从生成源头直接使用中文。
- 实际 UI 文案、ID、URL、代码、协议、产品/框架名称及 API、UI、DFX、Mock、Playwright、JMeter、String、Boolean、Number 等必要技术术语可以保留原文，不得为了中文化篡改真实页面文案或技术标识。
- 结构化内容完成后、写入 Excel 前集中执行一次语言自查并一次修正；不做自动翻译，不按单元格反复生成或重试。交付校验只作为遗漏审计，不能替代前置生成约束。

## 基本要求

- 正式测试设计交付物必须是 Excel。
- 任意测试设计都必须包含功能测试、性能测试、风险与待确认问题、自动化建议。
- 正式写测试用例前，必须先基于产品理解摘要、页面实探、文档和历史资产整理风险项；页面、DOM 或安全测试数据可验证项一律默认定向补探，不得询问用户是否需要深探；接口、日志、环境、权限或联调不足记录对应客观阻塞状态。`待确认` 只用于需求含义、业务规则、范围边界、角色职责或预期结果等需要用户解释或决策的理解问题。
- 待确认理解问题非空时，必须展示编号清单并结束当前轮次；收到用户后续明确回复前，不得进入 DFX、用例设计、结构化数据或 Excel 生成，把问题写入风险与待确认问题 Sheet 不等于用户已经确认。确认结果用于动态调整测试范围、测试数据、优先级、步骤、预期结果和风险等级。
- 用户确认、补充、排除或调整后，必须判断是否产生新的页面、元素、角色、状态、数据、规则或验证目标；如可通过页面、DOM 或安全测试数据验证，必须返回定向补探并更新风险与覆盖状态，不能直接进入 DFX 或用例生成。
- 有页面时，必须先完成全区域基线盘点、按业务流程动态深探、操作后新增元素补充和最终收口复查；未通过深探准出门禁的页面事实不得进入 DFX、测试场景或测试用例。
- 模块或批次正式写测试用例前，必须先把深探、文档、历史资产和用户说明中的事实按测试对象、角色/状态、动作/输入、数据、观察点和恢复路径形成候选原子场景；任一因素实际变化时先拆分，不得先按 DFX 选取代表性场景。
- 每项事实必须形成独立原子场景、记录合理合并依据，或明确落入性能测试设计、风险与待确认问题、自动化建议、不适用/不测范围；未完成事实核账不得开始 DFX，禁止事实静默消失。
- 完成原子化与核账后，再为每个场景标记一个主 DFX 维度与场景，并评估 DFX 12 维度 × 4 场景以补充适用的质量场景。DFX 只能标记或补充，不得抽样、删除或合并已形成的不同功能事实；新产生待确认理解问题时必须先完成跨轮次确认门禁。
- 敏捷用户故事每条至少 10 条功能测试用例，不得通过同质化用例凑数量。
- 用例必须可执行、可验证、可追踪，并关联需求 ID、功能点、`DFX维度`、`DFX场景` 和风险或验收标准。
- 测试用例必须尽可能详细。每个测试点、每个页面元素都要按主流程、异常流程、边界值、权限/角色、状态流转、数据一致性、组合条件、禁用态/空状态/错误态、兼容性/稳定性、性能影响、审计/日志/通知、副作用和可恢复路径展开，不能只写笼统用例。
- 异常值、边界值和测试策略不得只写一句话，必须基于 `dfx-test-strategy.md` 选择适用维度和场景，并落实到测试数据、操作步骤、预期结果和恢复路径。

## 编排与标题

- 功能测试用例必须按模块、页面、业务流程和小功能块组织。
- `功能点` 默认作为父场景和连续分组；其下按业务闭环展开不同测试点，不新增父场景维护字段。
- 同一小功能块的用例必须连续放在附近，例如新增、查看、编辑、删除、搜索、筛选、分页、导入、导出、审批等分别成组。
- 小功能块内部按 `DFX维度`、`DFX场景`、业务流程和页面元素连续编排；`场景类型`、`正向/反向` 不再作为测试策略主字段。
- `用例标题` 和导入文件中的 `测试用例名称` 必须正式、简洁、可检索，避免口语化。
- `用例标题` 必须使用 `功能点-当前用例标题` 格式，短横线前后不加空格，例如 `搜索筛选-按智能体名称查询列表`。

## 步骤与预期

- `前置条件`、`操作步骤`、`预期结果` 必须使用 `1. 2. 3.` 编号换行。
- 编号必须从 1 连续递增。关键操作与预期结果必须按执行顺序一一对应：每个触发、输入、提交/取消和结果观察都要有明确验证点，不得出现步骤执行 A、预期却描述 B，或同一用例同时声称互斥结果。
- `操作步骤` 不得默认执行人已经位于当前模块页面，必须从系统或项目入口开始写完整导航路径；导航统一写成 `一级菜单-二级菜单-目标页面`，层级之间使用半角短横线且前后不加空格，不使用 `[]`、`【】`、中文引号、`->`、`→` 或 `>` 包裹、连接菜单名称。
- 菜单、页面、按钮、字段、输入框、下拉框、页签、选项和图标名称默认直接书写，不使用括号或引号包裹，例如写 `点击创建按钮`，不写 `点击[创建]按钮`；页面真实提示写成 `页面提示：具体内容`，只有标点属于实际文案时才保留。
- 页面交互类用例必须写明具体页面元素、控件名称或入口文案。
- 弹窗、抽屉、下拉框、编辑态、删除确认框、添加变量、动态行、输入校验或联动选择等临时交互必须写完整闭环；步骤不能停在“点击按钮”“展开下拉”“尝试选择”“输入后观察”，必须继续写明确认、取消或关闭、返回列表、恢复输入或保持数据不变等可执行收尾。
- 页面交互用例直接复用深探得到的 `触发元素 → 状态出现 → 状态内操作 → 终态动作 → 页面/数据结果` 链路。若步骤从系统入口开始，不能在前置条件中假设弹窗、抽屉或编辑态已经打开；中间状态必须由步骤显式触发，终态动作必须写在操作步骤中，不能只写在预期结果中。
- 对已有数据进行查看、编辑预览、删除确认预览时，必须在步骤或预期中明确不保存、不提交、不最终确认，并写清取消或关闭路径。
- 预期结果必须可观察、可验证，并体现业务规则来源，例如状态变化、数据展示/落库、权限拦截、接口结果、通知、日志、审计或验收标准。
- 必须区分已实探结果、用户说明和设计预期：只有实际观察到的行为才能写成确定页面反馈；用户说明记录为业务依据；尚未执行的内容必须明确写为“设计预期，待执行验证”，不得推测具体提示文案、数量、排序方式、接口路径或前后端拦截位置。
- 预期结果不得使用“或者类似提示”“可能自动处理”“前端或后端任一路径”等方式掩盖证据不足；无法获得精确行为时，应保留可验证的业务目标并把未知部分登记为风险、待确认或联调事项。
- 每条用例必须形成可独立执行的小业务闭环：从入口和导航开始，完成前置状态或造数、关键动作、结果/数据/状态验证，并以取消、恢复、回滚或清理收尾。不同页面元素可以处在同一闭环中，不要求一个元素固定生成一条用例。
- 场景矩阵中的原子场景可以合并落到同一用例，但仅限 `功能点 + 前置条件 + 测试数据 + 操作步骤` 相同且 DFX 观察点可清晰对应的情况；角色、数据、状态、动作或恢复路径不同时必须拆分。
- 分页场景按 `pagination.md` 的实际能力和状态转换设计；首页、上一页、页码选择、下一页、末页、跳页以及不同页容量的动作、数据或观察点不同时不得合并。
- 每个 `是否生成用例=是` 的原子场景必须落入至少一条功能用例；一条用例承载多个相容场景时，步骤和预期必须分别覆盖其观察点。页面元素标记已覆盖时，关联用例必须覆盖实际交接的行为分支，不能只覆盖元素名称。
- 页面事实、文档事实和用户说明先进入结构化场景，再进入功能用例、导入文件和最终汇总；示例数据必须满足本用例输入条件，标题、测试数据、步骤和预期必须描述同一对象与同一结果。URL、菜单路径、提示文案和数量只能复用证据，不得自行拼接或补全。
- 完全重复场景必须在生成前合并；校验器报告的相似用例候选必须在交付前逐项判定为 `需合并`、`合理差异` 或 `校验误报`。不新增 Sheet 或字段，但未完成分类复核时不得声明交付完成。

## DFX 测试策略落地

每个小功能块至少考虑以下适用维度：

- DFT 功能：正向流程、边界值、异常输入、逆向操作。
- DFB 业务：业务流程、数据准确、端到端、报表统计。
- DFS 安全：身份认证、权限控制、数据脱敏、注入防护。
- DFR 可靠：故障恢复、数据一致、幂等性、断点续传。
- DFU 可用：操作便捷、错误提示、用户引导、操作反馈。
- DFP 性能：响应时间、并发处理、大数据量、资源监控；适用时写入 `性能测试设计` Sheet。

如果存在接口、兼容、维护、部署、运维或极端压测特征，还必须追加：

- DFI 接口：参数校验、协议兼容、错误码、超时重试。
- DFC 兼容：浏览器、操作系统、屏幕适配、数据格式。
- DFM 维护：配置热更新、灰度发布、回滚机制、日志追踪。
- DFD 部署：全新安装、版本升级、卸载回滚、配置迁移。
- DFO 运维：监控指标、告警配置、故障自愈、容量规划。
- DFX 极端：压力极限、破坏性、资源耗尽、并发极限。

无法在当前页面直接验证的 DFX 场景，应写入风险与待确认问题、性能测试设计或自动化建议，并说明需要的接口、日志、监控、环境或测试数据支持。

## 测试数据

- 如果用户提供测试数据，应优先写入或引用到 `测试数据` 字段。
- 正常数据用于确认成功路径、跳转结果、状态变化和页面展示。
- 异常、边界、非法、空值、重复、无权限等数据用于补充负向用例，并记录页面真实提示、校验位置和恢复路径。
- 数据不足只影响实探证据等级，不得减少页面已经识别出的测试场景和用例；无法安全造数时写明数据条件，并将未执行结果标记为设计预期，待执行验证。
