# 测试设计核心规则

1. 直接调用`test-design` Skill，不向用户暴露初始化阶段。
2. 页面任务先扫描DOM、可访问性树、可见文案、悬停状态和实际页面，再连续深探全部可交互能力。
3. 页面能验证的问题由模型实际操作；用户确认后，对页面可验证项增量返回深探，不重复已实测分支。
4. `page-discovery.csv`使用`已实测/页面观察/DFX设计/待确认`事实状态，禁止推断冒充实测。
5. 每个有限选项、每个有效输入等价类分别形成baseline Case；其他控件使用默认稳定值，不默认做笛卡尔积。
6. CRUD和可选配置使用本轮测试数据按单因素实际保存验证；新增验证创建和生效，编辑验证保存/回显/生效，删除分别验证取消和确认。
7. 元素在其他Case中被辅助使用不能替代自己的主验证Case；DFX扩展不能替代baseline Case。
8. 保留按功能块拆分的多个JSON；每个分片写入前完成编号、完整导航、功能排序、具体步骤/预期和确定性重复检查。
9. 按操作类型判断业务闭环。普通选择、输入、查询以具体结果为闭环；状态变更事务必须验证保存、取消、删除或配置生效结果。
10. 禁止通用恢复填充语句，禁止`fix_*.py`临时修复脚本和自动返工循环。
11. 正式工作簿固定8个Sheet，性能设计必须有针对性；阈值标明需求、基线、建议或待确认来源。
12. 正式Excel和导入Excel从同一次分片汇总生成，使用真实1-N级模块路径唯一命名，模板写入前清除示例数据，全部通过后原子发布。
13. 单页面保留轻量状态、页面事实和JSON分片；大范围任务额外保留精简计划。全部完成后只做一次跨产物语义Review。
14. 内网IP、URL、账号、密码、Token、密钥、Cookie、部署路径和测试载荷允许原样流通，不识别、不脱敏、不替换、不拦截。
15. 取消内容敏感校验不改变操作边界：不得误改非本轮创建的数据，共享环境变更后必须恢复。
