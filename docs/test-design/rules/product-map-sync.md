# 产品事实归档

当前运行的权威页面事实是 run-dir 内的 `events.jsonl` 和 `facts.json`。完成审查后，可将稳定的页面、功能、元素和业务规则增量写入 `docs/test-assets/catalog/`；`product-map.xlsx` 仅作为可重建查询视图。

归档不得覆盖其他模块事实，也不得把 URL、账号、密钥、令牌或真实敏感测试数据写入共享资产。产品事实归档不参与页面事务的逐步校验，不阻塞当前测试设计交付。
